{
  "version": 2,
  "builds": [
    {
      "src": "src/**/*",
      "use": "@vercel/staticbuild",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}